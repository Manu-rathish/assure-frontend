/** Full-width shell — stretches to viewport; min-w-0 prevents flex overflow. */
export const pageShellClass =
  "flex w-full min-w-0 flex-col gap-6 p-4 sm:p-6 lg:px-8";

/** Sticky chrome inner row (navbar, subnav). */
export const chromeInnerClass = "w-full min-w-0 px-4 sm:px-6 lg:px-8";
